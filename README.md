# genlayer-web-claim-verifier
WebClaimVerifier is a reusable GenLayer Intelligent Contract designed to verify real-world claims using decentralized AI consensus. Instead of relying on a single oracle or centralized data provider, the contract allows multiple validators to independently evaluate a claim and its supporting web sources.
